require("lsp-endhints").setup {
  icons = {
    -- The default config seems to use a random set of private-use code points for an unnamed custom
    -- font, which I don't bother to install.
    type = ": ",
    parameter = "",
    offspec = "",
    unknown = "",
  },
}
